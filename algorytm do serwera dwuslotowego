##okej to ja tak to sobie wyobrażam: (s1, s2 to server_release_time dla poszczególnych slotów)
if arrival_time >= s1 and arrival_time >= s2:  
    ##to co przed wpisaaniem nowego server_release_time
    s1 = arrival_time + pst
elif arrival_time >= s1:    
    ##to co przed wpisaaniem nowego server_release_time
    s1 = arrival_time + pst
    s2 = (s2 - arrival_time) / 2 #sprawdzic czy zgadzaja sie formaty liczb + 
    if s1 >= s2:
        s1 = s1 - (s1 - s2) / 2
    else:
        s2 = s2 - (s2 - s1) / 2
elif arrival_time >= s2:
    ##to co przed wpisaaniem nowego server_release_time
    s2 = arrival_time + pst
    s1 = (s1 - arrival_time) / 2 #sprawdzic czy zgadzaja sie formaty liczb + 
    if s1 >= s2:
        s1 = s1 - (s1 - s2) / 2
    else:
        s2 = s2 - (s2 - s1) / 2
else:
    ##drop packet
